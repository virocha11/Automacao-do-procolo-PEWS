# Em desenvolvimento com Vite, deixe vazio para usar o proxy (/api -> backend).
# Em produção ou se o proxy não for usado, aponte para a URL do backend, ex.:
# VITE_API_URL=http://localhost:3000

VITE_API_URL=
